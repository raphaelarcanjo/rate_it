{% extends 'layouts/base.html.twig' %} {% block content %}
<article>
    <p class="fs-4">Top 5 Entertainments</p>
    <hr />

    <div class="row">
        <div class="col-12 col-lg-3">
            <p class="fs-5 text-decoration-underline">Movies</p>
            {% if movies %}
                {{ include('layouts/elements/entertainment.html.twig', {entertainments: movies}) }}
                <a href="{{ path('movie') }}">View more...</a>
            {% else %}
                <p class="alert alert-info">Movies are not registered yet!</p>
            {% endif %}
        </div>

        <div class="col-12 col-lg-3">
            <p class="fs-5 text-decoration-underline">Musics</p>
            {% if musics %}
                {{ include('layouts/elements/entertainment.html.twig', {entertainments: musics}) }}
                <a href="{{ path('music') }}">View more...</a>
            {% else %}
                <p class="alert alert-info">Musics are not registered yet!</p>
            {% endif %}
        </div>

        <div class="col-12 col-lg-3">
            <p class="fs-5 text-decoration-underline">Books</p>
            {% if books %}
                {{ include('layouts/elements/entertainment.html.twig', {entertainments: books}) }}
                <a href="{{ path('book') }}">View more...</a>
            {% else %}
                <p class="alert alert-info">Books are not registered yet!</p>
            {% endif %}
        </div>

        <div class="col-12 col-lg-3">
            <p class="fs-5 text-decoration-underline">Games</p>
            {% if games %}
                {{ include('layouts/elements/entertainment.html.twig', {entertainments: games}) }}
                <a href="{{ path('game') }}">View more...</a>
            {% else %}
                <p class="alert alert-info">Games are not registered yet!</p>
            {% endif %}
        </div>
    </div>
</article>
{% endblock content %}
